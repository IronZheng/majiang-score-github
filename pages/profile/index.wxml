<view class="container">

  <view class="profile-header logined">
    <view class="user-info-row">
      <button class="avatar-lg avatar-btn" style="background:{{avatarBg}}" open-type="chooseAvatar" bindchooseavatar="onChooseAvatar">
        <image
          wx:if="{{userInfo.avatarUrl}}"
          class="avatar-image"
          src="{{userInfo.avatarUrl}}"
          mode="aspectFill"
        ></image>
        <text wx:else class="avatar-fallback">玩</text>
      </button>
      <view class="info-col">
        <view class="nickname-row">
          <input
            class="nickname-input"
            type="nickname"
            value="{{userInfo.nickName || '玩家'}}"
            placeholder="输入微信昵称"
            placeholder-class="nickname-placeholder"
            bindinput="onNicknameInput"
            bindconfirm="onNicknameConfirm"
            bindblur="onNicknameBlur"
          />
          <text class="edit-hint">✎</text>
        </view>
        <view class="profile-actions">
          <text class="reset-profile" bindtap="resetDefaultProfile">恢复默认</text>
        </view>
      </view>
    </view>
    <view class="profile-fade"></view>
  </view>

  <!-- 历史记录 -->
  <view class="card history-card">
    <view class="section-head">
      <text class="title">历史计分记录</text>
      <text class="history-count" wx:if="{{history.length}}">{{history.length}}条</text>
    </view>
    <block wx:if="{{history.length === 0}}">
      <text class="empty">暂无记录</text>
    </block>
    <block wx:for="{{history}}" wx:key="id">
      <view class="item" data-id="{{item.id}}" bindtap="openRecord">
        <view class="record-main">
          <text class="champion">冠军：{{item.ranking[0].name}}</text>
          <view class="score-row">
            <text class="score">{{item.ranking[0].score}}分</text>
            <text class="record-arrow">></text>
          </view>
        </view>
        <view class="record-sub">
          <view class="record-meta">
            <text class="badge">{{item.players.length}}人局</text>
            <text class="time">{{item.finishedAtText}}</text>
          </view>
          <view class="delete-record-btn" data-id="{{item.id}}" catchtap="deleteRecord">
            <image class="delete-record-icon" src="/assets/icons/delete-record.svg" mode="aspectFit"></image>
          </view>
        </view>
      </view>
    </block>
  </view>

  <view class="card menu-card">
    <view class="menu-item" bindtap="showAbout">
      <view class="menu-left">
        <text class="menu-symbol">i</text>
        <text class="menu-title">小程序说明</text>
      </view>
      <text class="menu-arrow">></text>
    </view>
    <view class="menu-item" bindtap="showFeedback">
      <view class="menu-left">
        <text class="menu-symbol">?</text>
        <text class="menu-title">意见反馈</text>
      </view>
      <text class="menu-arrow">></text>
    </view>
  </view>

</view>
